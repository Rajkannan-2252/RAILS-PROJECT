= render :partial => "layouts/flash_msg"
.col-md-12
  = react 'AttachDetachCloudNetworkForm', :recordId => @network.id.to_s,
                                          :isAttach => true,
                                          :dropdownChoices => @vm_choices.sort,
                                          :dropdownLabel => "Instance"  ## Label will be translated in schema